// next.config.mjs — {{PROJECT_NAME}}
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone', // image Docker runtime minimale (voir Dockerfile)
}

export default nextConfig
