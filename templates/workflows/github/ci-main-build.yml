name: ci-main-build
# Check complet (PR -> main) : build de production.
on:
  pull_request:
    branches: [main]
jobs:
  build:
    name: Build de production
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Installation
        run: make install
      - name: Build
        run: |
          echo "TODO : make build (next build / vite build + build back)"
